To use bvp6c, simply add it to of the MATLAB path 
(somewhere near the top).

There may be conflicts with bvp5c in the new version,
meaning you can't use both at the same time. If bvp5c
stops working, then remove bvp6c from the path.